@host = http://localhost:5004

### Головна — подивіться заголовки X-Correlation-ID та X-Powered-By, і лог із часом
GET {{host}}/

### Передаємо власний correlation id — сервер його підхопить
GET {{host}}/
X-Correlation-ID: my-trace-1
