@host = http://localhost:5013

### Нормальний потік — дивіться поле "steps" (порядок фільтрів)
GET {{host}}/api/demo/ok

### Виняток → перехоплює ExceptionFilter (500), у steps є "exception filter"
GET {{host}}/api/demo/boom
